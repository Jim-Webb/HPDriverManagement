# To Do

- Add the auto creation of required directories, maybe use some kind of initialization script to setup paths. 
- Look into weird permissions error on newly created paths
- Look into the auto creation/update of the PowerShell profile to setup entries for $PSDefaultParameters
- Add support for using Zip and Wim files for driver packs. Currently only the 7-Zip format is supported.
- Look into publishing to the PowerShell Gallery for easier install.